<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.evaluation.mapper.EvaluationDatasetCaseMapper">
    <insert id="insertBatch">
        INSERT INTO evaluation_dataset_case
            (id, dataset_version_id, test_case_version_id, sort_order, enabled, created_at)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.datasetVersionId}, #{entity.testCaseVersionId},
             #{entity.sortOrder}, #{entity.enabled}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>
</mapper>
