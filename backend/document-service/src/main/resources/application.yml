server:
  port: ${DOCUMENT_SERVICE_PORT:8082}

spring:
  application:
    name: document-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
