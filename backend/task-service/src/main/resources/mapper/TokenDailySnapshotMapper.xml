<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.task.mapper.TokenDailySnapshotMapper">
    <insert id="insertBatch">
        INSERT INTO token_daily_snapshot
            (id, space_id, snapshot_date, total_input, total_output,
             total_estimated_cost, snapshot_type, created_at)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.spaceId}, #{entity.snapshotDate}, #{entity.totalInput},
             #{entity.totalOutput}, #{entity.totalEstimatedCost}, #{entity.snapshotType},
             CURRENT_TIMESTAMP)
        </foreach>
    </insert>
</mapper>
