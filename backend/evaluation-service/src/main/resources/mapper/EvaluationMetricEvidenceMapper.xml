<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.evaluation.mapper.EvaluationMetricEvidenceMapper">
    <insert id="insertBatch">
        INSERT INTO evaluation_metric_evidence
            (id, metric_id, evidence_reference_id, created_at)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.metricId}, #{entity.evidenceReferenceId}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>
</mapper>
