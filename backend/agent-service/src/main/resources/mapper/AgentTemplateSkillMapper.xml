<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.agent.mapper.AgentTemplateSkillMapper">

    <insert id="insertBatch">
        INSERT INTO agent_template_skill
            (id, template_version_id, skill_id, skill_version_id, created_at)
        VALUES
        <foreach collection="references" item="reference" separator=",">
            (#{reference.id}, #{reference.templateVersionId}, #{reference.skillId},
             #{reference.skillVersionId}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>
</mapper>
