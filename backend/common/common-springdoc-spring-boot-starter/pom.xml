<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.agentdoc</groupId>
        <artifactId>agent-doc-common</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <artifactId>agent-doc-common-springdoc-spring-boot-starter</artifactId>
    <name>agent-doc-common-springdoc-spring-boot-starter</name>
    <description>SpringDoc OpenAPI 自动配置：统一 OpenAPI 文档模板（bearerAuth 安全方案，title/description 可配置）</description>

    <dependencies>
        <!-- 公共核心库 -->
        <dependency>
            <groupId>com.agentdoc</groupId>
            <artifactId>agent-doc-common-core</artifactId>
        </dependency>
        <!-- SpringDoc（MVC 版）：本 starter 直接携带，业务服务零配置获得 OpenAPI/Swagger UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <!-- Spring Boot AutoConfiguration：@AutoConfiguration / 条件注解 / 配置属性绑定 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <!-- Lombok：编译用；provided 不进入消费者传递依赖 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
