<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.evaluation.mapper.EvaluationCaseRunMapper">
    <insert id="insertBatch">
        INSERT INTO evaluation_case_run
            (id, run_id, space_id, test_case_version_id, status, current_attempt_id,
             created_at, updated_at)
        VALUES
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.runId}, #{entity.spaceId}, #{entity.testCaseVersionId},
             #{entity.status}, #{entity.currentAttemptId}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
        </foreach>
    </insert>

    <update id="updateBatch">
        UPDATE evaluation_case_run
        SET status = CASE id
            <foreach collection="entities" item="entity">
                WHEN #{entity.id} THEN #{entity.status}
            </foreach>
            END,
            updated_at = CURRENT_TIMESTAMP
        WHERE id IN
        <foreach collection="entities" item="entity" open="(" separator="," close=")">
            #{entity.id}
        </foreach>
    </update>
</mapper>
