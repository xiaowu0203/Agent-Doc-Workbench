<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agentdoc.agent.mapper.AgentTemplateMcpMapper">

    <insert id="insertBatch">
        INSERT INTO agent_template_mcp
            (id, template_version_id, mcp_template_id, mcp_template_version_id,
             tool_whitelist_json, created_at)
        VALUES
        <foreach collection="references" item="reference" separator=",">
            (#{reference.id}, #{reference.templateVersionId}, #{reference.mcpTemplateId},
             #{reference.mcpTemplateVersionId}, #{reference.toolWhitelistJson}, CURRENT_TIMESTAMP)
        </foreach>
    </insert>
</mapper>
